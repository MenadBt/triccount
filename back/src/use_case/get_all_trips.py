from application.trip.trip_api import TripApi
from dependencies import trip_repo


def get_all_trips():
    # trips_db = trip_repo.get_all()
    #
    # # Maps the list of TripDb to a list of TripApi
    # trips_api = [TripApi.from_trip_db(trip_db) for trip_db in trips_db]
    #
    # return trips_api
    return []
